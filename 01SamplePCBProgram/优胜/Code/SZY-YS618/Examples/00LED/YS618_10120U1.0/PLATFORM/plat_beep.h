#ifndef __PLAT_BEEP_H
#define __PLAT_BEEP_H

#include "main.h"

/* ?????????????????? 2kHz */
#define BEEP_FREQ_HZ  2000u

// ?????Ò‘???/?????????????? BEEP_FREQ_HZ??
typedef enum {
    BEEP_CLICK = 0,    // ??? 50ms (????)
    BEEP_LIMIT,        // ??? 200ms (?????)
    BEEP_ALARM,        // 100ms ?? / 100ms ? ??10 (????)
    BEEP_POWER_ON,     // ???? 400ms (???)
    BEEP_PROG_SHIFT,   // P ≥Ã Ω∂Œ«–ªª£∫≥§µŒ ~220ms
} BeepMode_t;
void Beep_Init(void);
void Beep_Handler_1ms(void); 
void Beep_Play(BeepMode_t mode); // ???????
void Beep_Play_Custom(uint16_t freq, uint8_t vol, uint16_t on, uint16_t off, uint8_t count);

#endif
